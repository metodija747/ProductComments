apiVersion: v1
kind: Service
metadata:
  name: comment-microservice
spec:
  selector:
    app: comment-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
